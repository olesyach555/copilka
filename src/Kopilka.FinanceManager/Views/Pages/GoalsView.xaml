<UserControl x:Class="Kopilka.FinanceManager.Views.Pages.GoalsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Финансовые цели" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" />
        <ListBox Grid.Row="1" ItemsSource="{Binding Goals}" HorizontalContentAlignment="Stretch">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="5" Padding="10" CornerRadius="5">
                        <StackPanel>
                            <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" />
                            <Grid Margin="0,5">
                                <ProgressBar Minimum="0" Maximum="{Binding TargetAmount}" Value="{Binding CurrentAmount}" Height="20" />
                                <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center">
                                    <Run Text="{Binding CurrentAmount}" />
                                    <Run Text=" / " />
                                    <Run Text="{Binding TargetAmount}" />
                                </TextBlock>
                            </Grid>
                            <TextBlock Text="{Binding TargetDate, StringFormat='Цель к: {0:dd.MM.yyyy}'}" FontSize="12" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
