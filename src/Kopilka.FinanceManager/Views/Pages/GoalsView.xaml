<UserControl x:Class="Kopilka.FinanceManager.Views.Pages.GoalsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Финансовые цели" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="+ Новая цель" Command="{Binding AddGoalCommand}" Padding="10,5" />
        </StackPanel>

        <ItemsControl Grid.Row="2" ItemsSource="{Binding Goals}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Margin="0,0,0,10" Padding="10" CornerRadius="5" Background="White">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <StackPanel>
                                <TextBlock Text="{Binding Name}" FontSize="16" FontWeight="Bold" />
                                <TextBlock Text="{Binding TargetDate, StringFormat='Срок: {0:dd.MM.yyyy}'}" Foreground="Gray" />
                                <ProgressBar Value="{Binding CurrentAmount}" Maximum="{Binding TargetAmount}" Height="20" Margin="0,10,0,5" />
                                <TextBlock Text="{Binding CurrentAmount, StringFormat='Накоплено: {0} из {1}'}" HorizontalAlignment="Center" />
                            </StackPanel>
                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="20,0,0,0">
                                <Button Content="Пополнить" Command="{Binding DataContext.AddFundsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="0,0,0,5" />
                                <Button Content="Изменить" Command="{Binding DataContext.EditGoalCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="0,0,0,5" />
                                <Button Content="Удалить" Command="{Binding DataContext.DeleteGoalCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>
