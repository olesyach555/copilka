<UserControl x:Class="Kopilka.FinanceManager.Views.Pages.TransactionHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Журнал транзакций" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

        <!-- Фильтры -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="С:" VerticalAlignment="Center" Margin="0,0,5,0" />
            <DatePicker SelectedDate="{Binding StartDate}" Width="120" />
            <TextBlock Text="По:" VerticalAlignment="Center" Margin="10,0,5,0" />
            <DatePicker SelectedDate="{Binding EndDate}" Width="120" />
            <Button Content="Обновить" Command="{Binding LoadDataCommand}" Margin="10,0,0,0" Padding="10,2" />
            <Button Content="Показать график" Command="{Binding ShowChartCommand}" Margin="10,0,0,0" Padding="10,2" />
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Transactions}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding Date, StringFormat={}{0:dd.MM.yyyy}}" Width="100" />
                <DataGridTextColumn Header="Сумма" Binding="{Binding Amount}" Width="100" />
                <DataGridTextColumn Header="Категория" Binding="{Binding Category.Name}" Width="150" />
                <DataGridTextColumn Header="Комментарий" Binding="{Binding Comment}" Width="*" />
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <TextBlock Text="Общий баланс: " VerticalAlignment="Center" />
            <TextBlock Text="{Binding TotalBalance}" FontWeight="Bold" VerticalAlignment="Center" Margin="5,0,0,0" />
        </StackPanel>
    </Grid>
</UserControl>
