<Page x:Class="Kopilka.FinanceManager.Views.Pages.CategoriesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Kopilka.FinanceManager.Views.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CategoriesPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="20,10">
            <TextBlock Text="Управление категориями" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
            <Button Content="Добавить категорию" Style="{StaticResource PrimaryButton}" Click="AddCategoryButton_Click" Margin="20,0,0,0"/>
        </StackPanel>

        <ListView Grid.Row="1" x:Name="CategoriesListView" Margin="20,0,20,20">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Название" DisplayMemberBinding="{Binding Name}" Width="200"/>
                    <GridViewColumn Header="Тип" DisplayMemberBinding="{Binding Type}" Width="100"/>
                    <GridViewColumn Header="Действия" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Button Content="Изм." Style="{StaticResource SecondaryButton}" Click="EditButton_Click" Padding="5,2"/>
                                    <Button Content="Удалить" Click="DeleteButton_Click" Margin="5,0,0,0" Padding="5,2"/>
                                </StackPanel>
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</Page>
