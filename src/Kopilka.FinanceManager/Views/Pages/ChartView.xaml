<UserControl x:Class="Kopilka.FinanceManager.Views.Pages.ChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Text="Гистограмма доходов и расходов" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" />

        <Canvas x:Name="ChartCanvas" Grid.Row="1" Margin="20" Background="#f8f9fa" />
    </Grid>
</UserControl>
