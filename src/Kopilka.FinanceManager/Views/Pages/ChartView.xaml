<UserControl x:Class="Kopilka.FinanceManager.Views.Pages.ChartView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Назад" Command="{Binding DataContext.NavigateToTransactionsCommand, RelativeSource={RelativeSource AncestorType=Window}}" Margin="0,0,10,0" />
            <TextBlock Text="График доходов и расходов" FontSize="18" FontWeight="Bold" />
        </StackPanel>

        <Canvas x:Name="ChartCanvas" Grid.Row="1" Background="#F0F0F0" />
    </Grid>
</UserControl>
