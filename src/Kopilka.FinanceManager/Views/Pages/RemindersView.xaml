<UserControl x:Class="Kopilka.FinanceManager.Views.Pages.RemindersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="Transparent">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Напоминания" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" />

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="+ Напоминание" Command="{Binding AddReminderCommand}" Padding="10,5" />
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding Reminders}" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Дата" Binding="{Binding ReminderDate, StringFormat={}{0:dd.MM.yyyy}}" Width="100" />
                <DataGridTextColumn Header="Заголовок" Binding="{Binding Title}" Width="*" />
                <DataGridTextColumn Header="Категория" Binding="{Binding TransactionCategory.Name}" Width="150" />
                <DataGridCheckBoxColumn Header="Повтор" Binding="{Binding IsRecurring}" Width="70" />
                <DataGridTemplateColumn Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Изм." Command="{Binding DataContext.EditReminderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" Margin="0,0,5,0" />
                                <Button Content="Удал." Command="{Binding DataContext.DeleteReminderCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" CommandParameter="{Binding}" />
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
