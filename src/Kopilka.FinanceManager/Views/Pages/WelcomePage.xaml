<Page x:Class="Kopilka.FinanceManager.Views.Pages.WelcomePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:Kopilka.FinanceManager.Views.Pages"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="WelcomePage">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Добро пожаловать в Kopilka Finance Manager!" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" TextWrapping="Wrap" Margin="0,0,0,10">
            Это ваш начальный экран с примерами транзакций. Ниже вы видите, как могут выглядеть ваши записи о доходах и расходах.
        </TextBlock>

        <ListView Grid.Row="2" Name="TransactionsListView">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Дата" DisplayMemberBinding="{Binding Date, StringFormat='dd.MM.yyyy'}" />
                    <GridViewColumn Header="Сумма" DisplayMemberBinding="{Binding Amount}" />
                    <GridViewColumn Header="Категория" DisplayMemberBinding="{Binding Category.Name}" />
                    <GridViewColumn Header="Комментарий" DisplayMemberBinding="{Binding Comment}" />
                    <GridViewColumn Header="Счет" DisplayMemberBinding="{Binding Account.Name}" />
                    <GridViewColumn Header="Тип" DisplayMemberBinding="{Binding Type}" />
                </GridView>
            </ListView.View>
        </ListView>

        <TextBlock Grid.Row="3" TextWrapping="Wrap" Margin="0,10,0,0">
            <Run Text="Чтобы добавить новую транзакцию, нажмите на кнопку 'Добавить транзакцию' (обычно находится на главной панели)."/>
            <LineBreak/>
            <Run Text="Вы можете управлять своими счетами и категориями в соответствующих разделах, доступных из главного меню."/>
        </TextBlock>
    </Grid>
</Page>
